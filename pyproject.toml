[project]
name = "codyssey-b3-1"
version = "0.1.0"
description = "CLI Mini Redis assignment with custom data structures"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
